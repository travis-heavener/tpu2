section .text
__UF0:
    mov AX, 14336
    pushw AX
    mov AX, SP
    sub AX, 2
    pushw 0(AX)
    mov AX, 0
    popw CX
    add CX, AX
    pushw CX
    pushw 255
    mov AX, 51200
    pushw AX
    mov BX, 3
    popw AX
    sub AX, BX
    mov BX, AX
    popw AX
    and AX, BX
    pushw AX
    sub SP, 1
    pop BL
    xor BH, BH
    popw AX
    mov 0(AX), BL
    push BL
    sub SP, 1
    mov AX, SP
    sub AX, 2
    pushw 0(AX)
    mov AX, 1
    popw CX
    add CX, AX
    pushw CX
    pushw 255
    mov AX, 51200
    pushw AX
    mov BX, 3
    popw AX
    sub AX, BX
    pushw AX
    mov BX, 8
    popw AX
    shr AX, BL
    mov BX, AX
    popw AX
    and AX, BX
    pushw AX
    sub SP, 1
    pop BL
    xor BH, BH
    popw AX
    mov 0(AX), BL
    push BL
    sub SP, 1
    mov AX, SP
    sub AX, 2
    pushw 0(AX)
    mov AX, 2
    popw CX
    add CX, AX
    pushw CX
    pushw 0
    mov AL, -1(SP)
    and AL, 0x80
    sub SP, 1
    pop BL
    or BL, AL
    xor BH, BH
    popw AX
    mov 0(AX), BL
    push BL
    sub SP, 1
    sub SP, 2
    __UF0E:
    ret
_main:
    add SP, 2
    call __UF0
    pushw 19
    pushw 12312
    pushw 12312
    mov AX, -4(SP)
    pushw AX
    sub SP, 2
    mov AX, -2(SP)
    pushw AX
    sub SP, 2
    mov AX, SP
    sub AX, 4
    pushw AX
    pushw -4(SP)
    mov BX, -8(SP)
    popw AX
    add AX, BX
    mov BX, AX
    popw AX
    mov 0(AX), BL
    mov 1(AX), BH
    pushw BX
    sub SP, 2
    pushw -4(SP)
    mov BX, -8(SP)
    popw AX
    add AX, BX
    pushw AX
    pop DL
    mov -8(SP), DL
    pop DL
    mov -8(SP), DL
    sub SP, 6
    _mainE:
    mov AX, 0x03
    popw BX
    syscall
    hlt
section .data
