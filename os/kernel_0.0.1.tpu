section .text
__UF0:
    mov AX, 14336
    pushw AX
    mov BP, SP
    sub BP, 2
    push 0(BP)
    push 1(BP)
    mov AX, 0
    popw CX
    add CX, AX
    pushw CX
    pushw 255
    mov AX, 51200
    pushw AX
    mov BX, 3
    popw AX
    sub AX, BX
    mov BX, AX
    popw AX
    and AX, BX
    pushw AX
    sub SP, 1
    pop BL
    xor BH, BH
    popw AX
    mov BP, AX
    mov 0(BP), BL
    push BL
    sub SP, 1
    mov BP, SP
    sub BP, 2
    push 0(BP)
    push 1(BP)
    mov AX, 1
    popw CX
    add CX, AX
    pushw CX
    pushw 255
    mov AX, 51200
    pushw AX
    mov BX, 3
    popw AX
    sub AX, BX
    pushw AX
    mov BX, 8
    popw AX
    shr AX, BL
    mov BX, AX
    popw AX
    and AX, BX
    pushw AX
    sub SP, 1
    pop BL
    xor BH, BH
    popw AX
    mov BP, AX
    mov 0(BP), BL
    push BL
    sub SP, 1
    mov BP, SP
    sub BP, 2
    push 0(BP)
    push 1(BP)
    mov AX, 2
    popw CX
    add CX, AX
    pushw CX
    pushw 0
    mov AL, -1(SP)
    and AL, 0x80
    sub SP, 1
    pop BL
    or BL, AL
    xor BH, BH
    popw AX
    mov BP, AX
    mov 0(BP), BL
    push BL
    sub SP, 1
    sub SP, 2
    __UF0E:
    ret
_main:
    add SP, 2
    call __UF0
    pushw 19
    pushw 12312
    pushw 12312
    push -4(SP)
    push -4(SP)
    sub SP, 2
    push -2(SP)
    push -2(SP)
    sub SP, 2
    mov BP, SP
    sub BP, 4
    pushw BP
    push -4(SP)
    push -4(SP)
    push -8(SP)
    push -8(SP)
    popw BX
    popw AX
    add AX, BX
    mov BX, AX
    popw AX
    mov BP, AX
    mov 0(BP), BL
    mov 1(BP), BH
    pushw BX
    sub SP, 2
    push -4(SP)
    push -4(SP)
    push -8(SP)
    push -8(SP)
    popw BX
    popw AX
    add AX, BX
    pushw AX
    pop DL
    mov -8(SP), DL
    pop DL
    mov -8(SP), DL
    sub SP, 6
    _mainE:
    mov AX, 0x03
    popw BX
    syscall
    hlt
section .data
