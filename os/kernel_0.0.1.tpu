section .text
__UF0:
    movw AX, 14336
    pushw AX
    movw BP, SP
    sub BP, 2
    push [BP+0]
    push [BP+1]
    movw AX, 0
    popw CX
    add CX, AX
    pushw CX
    pushw 255
    movw AX, 51200
    pushw AX
    movw BX, 3
    popw AX
    sub AX, BX
    movw BX, AX
    popw AX
    and AX, BX
    pushw AX
    sub SP, 1
    pop BL
    xor BH, BH
    popw AX
    movw BP, AX
    mov [BP+0], BL
    push BL
    sub SP, 1
    movw BP, SP
    sub BP, 2
    push [BP+0]
    push [BP+1]
    movw AX, 1
    popw CX
    add CX, AX
    pushw CX
    pushw 255
    movw AX, 51200
    pushw AX
    movw BX, 3
    popw AX
    sub AX, BX
    pushw AX
    movw BX, 8
    popw AX
    shr AX, BL
    movw BX, AX
    popw AX
    and AX, BX
    pushw AX
    sub SP, 1
    pop BL
    xor BH, BH
    popw AX
    movw BP, AX
    mov [BP+0], BL
    push BL
    sub SP, 1
    movw BP, SP
    sub BP, 2
    push [BP+0]
    push [BP+1]
    movw AX, 2
    popw CX
    add CX, AX
    pushw CX
    pushw 0
    mov AL, [SP-1]
    and AL, 0x80
    sub SP, 1
    pop BL
    or BL, AL
    xor BH, BH
    popw AX
    movw BP, AX
    mov [BP+0], BL
    push BL
    sub SP, 1
    sub SP, 2
    __UF0E:
    ret
_main:
    add SP, 2
    call __UF0
    pushw 0
    pop DL
    mov [SP-2], DL
    pop DL
    mov [SP-2], DL
    _mainE:
    movw AX, 0x03
    popw BX
    syscall
    hlt
section .data
