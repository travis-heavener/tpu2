main:
    ; move bytes onto stack
    push 0x0A
    push 0xF8
    push 0x11

    ; move value relative to stack ptr into register
    rmov AL, $-2
    rmov AH, $+1

    hlt                             ; stop the clock